import React, { Component } from 'react';
import { Tile, List, ListItem, Button, Text } from 'react-native-elements';

class Weather extends Component {render() {
        return (
            <Text> Hava Durumu </Text>
        );
    }
}

export default Weather;
